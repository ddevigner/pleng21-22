procedure hello is
    character c[5];
    character b;
    integer a;
    boolean g;
    procedure hola(ref integer b, c) is
        integer a;
    begin
        a := a + 1;
    end
begin
    if g then
        g := false;
    end
    get(b,a,g);
    g := not false;
    c[0] := c[0];
    a := char2int(b);
    b := int2char(a);
    end
end